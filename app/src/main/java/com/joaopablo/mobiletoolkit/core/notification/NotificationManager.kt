package com.joaopablo.mobiletoolkit.core.notification

import android.content.Context

/**
 * Manager to send local notifications and setup Firebase Push Notifications.
 * To be implemented in detail during Notification integration phase.
 */
class NotificationManager(private val context: Context) {
    fun createNotificationChannel() {
        // Placeholder implementation
    }

    fun sendLocalNotification(title: String, message: String) {
        // Placeholder implementation
    }
}
